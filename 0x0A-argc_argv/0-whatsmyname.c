#include <stdio.h>
#include <stdlib.h>
/**
 * main - see description
 * Description: entry to main application
 * @agrc: input parameter for main
 * @argv: input vector for main
 * Return: 0 or 1
 */
int main(int agrc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
