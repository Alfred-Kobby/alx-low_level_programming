#include "main.h"
/**
 * _abs - Check description
 * Description: prints the absolute value
 * @c: input parameter for _abs function
 * Return: return the abloute value
 */
int _abs(int c)
{
	if (c >= 0)
		return (c);
	else
		return (c * -1);
}
