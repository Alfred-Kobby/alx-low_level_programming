#include "main.h"
/**
 * mul - check description
 * Description: returns a multiple of a nad b
 * @a: first input parameter for mul
 * @b: second input parameter for mul
 * Return: return a*b
 */
int mul(int a, int b)
{
	return (a*b);
}
